[
  {
    "stepTitle": "Upgrade",
    "items": [
      {
        "type": "tips",
        "helpText": "Hermes Studio will stop existing Node.js and Python bridge processes before upgrading. Keep the current Web UI port unless you want to move the service."
      },
      {
        "type": "text",
        "field": "wizard_service_port",
        "label": "Web UI port",
        "initValue": "6060",
        "rules": [
          {
            "required": true,
            "message": "Please enter the Web UI port."
          },
          {
            "pattern": "^[0-9]+$",
            "message": "The port must be numeric."
          }
        ]
      }
    ]
  }
]
